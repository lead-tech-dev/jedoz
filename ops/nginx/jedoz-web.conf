server {
  listen 80;
  server_name jedoz.com www.jedoz.com;

  root /var/www/jedoz/prod/web;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}

server {
  listen 80;
  server_name admin.jedoz.com;

  root /var/www/jedoz/prod/admin;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}

server {
  listen 80;
  server_name staging.jedoz.com;

  root /var/www/jedoz/staging/web;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}

server {
  listen 80;
  server_name admin.staging.jedoz.com;

  root /var/www/jedoz/staging/admin;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}

server {
  listen 80;
  server_name dev.jedoz.com;

  root /var/www/jedoz/dev/web;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}

server {
  listen 80;
  server_name admin.dev.jedoz.com;

  root /var/www/jedoz/dev/admin;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}
